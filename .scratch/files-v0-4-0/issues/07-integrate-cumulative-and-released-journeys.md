# 07 — Integrate cumulative and released Files journeys

**What to build:** Demonstrate realistic Files configuration alongside every prior surface in cumulative Northstar and prepare the complete registry-installed, source-migrated released journey before publication.

**Blocked by:** 01 — Validate candidate compatibility before mutation; 02 — Accept clones and worktrees as exact provenance; 03 — Revalidate stale registry version responses; 05 — Deliver the Files module and executable documentation; 06 — Prove and maintain the protected Files shard.

**Status:** resolved

- [x] The cumulative root manages at least two explicit folder levels and two tiny inert Managed files through composable stable-keyed module instances.
- [x] The desired state includes a private file and a public non-indexable file with reviewed source SHA-256 values and no implicit folder path.
- [x] Northstar uses a distinct protected credential containing the union of every released surface scope including `files`; single-surface credentials remain separate.
- [x] Candidate compatibility and clone/worktree provenance pass before any protected Northstar mutation.
- [x] Local candidate mode under both OpenTofu and Terraform covers reviewed plan/apply, canonical verification, empty plan, metadata/access drift, remote content drift, repair, import/adoption by exact IDs, folder path refresh, byte replacement, destroy plan/apply, and terminal cleanup.
- [x] File and folder generated IDs are read through stable module outputs and never recovered by name, path, URL, or search adoption.
- [x] Teardown is file-first and leaf-first and proves zero active Northstar-owned Files configuration while preserving all prior cleanup semantics.
- [x] CRM property schema, Form definition, state upgrades, provider registrations, modules, and prior Northstar journeys remain cumulative under both engines.
- [x] The released journey tooling is complete and hermetically testable before publication; it accepts the requested release version and exact provider/demo provenance.
- [x] Released verification waits for both ordinary registry endpoints using the bounded revalidation contract and then installs the matching provider through the correct engine/source pair.
- [x] Each registry-selected archive digest is bound to the immutable GitHub checksum inventory and mismatched engines, sources, versions, locks, or digests fail before live mutation.
- [x] One released hierarchy and Managed file preserve the same generated IDs as state moves from Terraform's provider source to OpenTofu's source and back.
- [x] Both engines read, plan, update, replace bytes, inject and repair drift, and reach empty plans without creating a second identity or destroying the fixture early.
- [x] The released cumulative Northstar journey runs under both engines after package verification.
- [x] Contract tests cover ordering, early failure, stale registry responses, identity preservation, state migration, cleanup traps, wrong version, and incomplete evidence without requiring publication or a live portal.

## Answer

Integrated two explicit folder levels and two SHA-bound Managed files into the cumulative demo and exact-ID Northstar lifecycle. Added the pre-publication `v0.4.0` Terraform-to-OpenTofu-to-Terraform Files migration, immutable checksum binding, fresh registry-lock preparation, sanitized terminal evidence, and fail-closed contract tests while preserving every prior surface under both engines.
