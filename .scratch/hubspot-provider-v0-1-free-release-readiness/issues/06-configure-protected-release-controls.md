# Configure the protected v0.1.0 release controls

Type: task
Status: open
Blocked by: 02

## Question

What repository remote, protected branch/environment, registry identities,
signing/provenance settings, scoped secrets, immutable-action checks, and
human-triggered workflow inputs must be configured and verified so Jack can publish
the qualified v0.1.0 artifact with one deliberate approval rather than ad hoc
release work?
